#defaults:
    #- override hydra/launcher: submitit_slurm

experiment_name: "eval_gp_14L"
strict_reload: True
reload_state_dict_path: <Path to GP model>/GP_model_state_dict.pth
pca_model_path: <Path to PCA model>/pca_model.sav


hydra:
    run:
        dir: ./results/${experiment_name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
############

model_cfg:
    _target_: src.models.ExactGP.GPRegressionModel
    lr: 0.001
    num_epochs: 1000
    inducing_points: null

feat_cfg:
    _target_: src.models.feat_extraction.BertFeatExtractor
    model_config_file: <Full Path>/biotransfer/pretrained_models/bert_configs/config.json
    feat_path: <Full Path>/biotransfer/pretrained_models/pfam.ckpt # Full path to the pretrained langauge model checkpoint
    strict_reload: False
    variable_regions: True
    pca_dim: 1024

train_set_cfg:
    _target_: src.datasets.CovidDataset
    data_path: <Full Path>/biotransfer/data/covid/
    split: train
    chain: 14L
    average_replicates: True
    add_static_ends: True
    correction: replicate
    filter_nan: median
    
train_dataloader_cfg:
    batch_size: 32
    shuffle: True
    
eval_set_cfg:
    _target_: src.datasets.CovidDataset
    data_path: <Full Path>/biotransfer/data/covid/
    split: test
    chain: 14L
    average_replicates: True
    add_static_ends: True
    correction: replicate
    filter_nan: median
    
eval_dataloader_cfg:
    batch_size: 32

